MNEMONIC=
DATABASE_URL=postgres://msgboard-ui-postgres:password@0.0.0.0:9876/msgboard-ui-postgres

# Per-chain RPC overrides (optional). When unset, the UI defaults to the valve.city
# vk_demo endpoints, which serve the msgboard_ module. Only point a chain at another
# node if that node runs the module (ordinary public RPCs do not).
# VITE_RPC_943=https://one.valve.city/rpc/vk_demo/evm/943
# VITE_RPC_369=https://one.valve.city/rpc/vk_demo/evm/369
# VITE_RPC_1=https://one.valve.city/rpc/vk_demo/evm/1
