# Copy to inventory.ini (gitignored) and fill in real hosts/users/keys.

# The dedicated Waku box (nwaku WSS node + optional relay).
[waku]
waku-1 ansible_host=88.99.62.98 ansible_user=root ansible_ssh_private_key_file=~/.ssh/REPLACE

# The msgboard prod box (already runs ui/hasura/etc.) — host its IP/name; the cosign UI deploys here.
[msgboard]
msgboard-1 ansible_host=REPLACE_MSGBOARD_BOX_IP ansible_user=root ansible_ssh_private_key_file=~/.ssh/REPLACE
