.env
node_modules
dist
postgres-data
deploy/caddy/origin.pem
deploy/caddy/origin.key
deploy/ansible/.vault_pass
deploy/ansible/group_vars/vault.yml

# visual brainstorm artifacts
.superpowers
progress.txt

# Serena MCP project cache + local-only root scripts
.serena/
/scripts/
packages/foundry/out/
packages/foundry/cache/
packages/foundry/lib/

# Yarn artifacts: this repo is npm-managed (package-lock.json is the lockfile).
# Reject stray Yarn 4 / Plug'n'Play files so a `yarn` run can't be committed.
.yarn/
.pnp.*
yarn.lock

# --- excluded from the public valve-tech repo (kept local) ---
# private deployment infra (msgboard.xyz ops)
deploy/
# infra-heavy planning docs (box/ssh/root details)
docs/superpowers/specs/2026-06-01-msgboard-faucet-deploy-design.md
docs/superpowers/plans/2026-06-01-msgboard-faucet-deploy.md
